<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @type.name %>
</p>

<p>
  <strong>Type:</strong>
  <%= @type.type_name %>
</p>

<h2>hardware</h2>
<% if @type.hardwares.any?  %>
<p>
	<% @type.hardwares.each do |l| %>
	
	<%= l.serie.to_s %> <br>
	<%= l.status%>
	
<p>
<%end%>
<%else%>
<p> no hay hardwar ingresados</p>
<%end%>
<br>

<%= link_to 'Edit', edit_type_path(@type) %> |
<%= link_to 'Back', types_path %>
